# 重新授权请求

请先完成网页端登录和授权，并在授权成功后写出 `output/current_auth_state.json`。

要求：
1. 这个文件必须写到当前工作目录的 `output/` 下
2. 至少包含 `authorized=true` 或 `connected=true`
3. 不要生成假文件
4. 如果授权没有完成，不要继续跑周复盘
5. 授权成功后，再继续执行周复盘任务
