Windows 侧唯一入口。直接按顺序执行下面三段内容：
=== 任务 ===
你在 Windows 机器上的 Accio 环境里，直接完成本机桥接。

必须完成：
1. 找到授权状态文件，确认已连接 / 已授权。
2. 找到网页端店铺与 Accio 配对店铺的对应关系。
3. 写入或更新 `output/current_shop_context.json`。
4. 验证周报脚本能读取这个文件。

硬性规则：
- 不要猜店铺 ID。
- 不要猜目录。
- 不要写 Mac 路径。
- 授权文件缺失、损坏、未连接时先停下并要求重新授权。
- 店铺映射找不到时先列证据，不要写假值。

按这个顺序做：
1. 检查 `current_auth_state.json` 或等价标记。
2. 检查店铺映射 / 当前店铺上下文。
3. 确认周报源目录和输出目录。
4. 写入 `output/current_shop_context.json`。
5. 验证脚本能读取它。

采集证据时运行 `windows_evidence_commands.txt`。
整理结果时用 `windows_result_template.txt`。
如果缺字段，回报四项：授权文件路径、店铺映射证据、写入的 JSON、是否还缺店铺码或目录。

=== 证据采集命令 ===
# Windows 侧 Accio / 周复盘证据采集命令
# 目标：先确认授权状态、店铺映射、周报源目录，再交给 Codex 写 current_shop_context.json

Write-Host "=== ACCIO ROOT ==="
Get-ChildItem "$env:APPDATA\Accio" -Force |
  Select-Object FullName, Length, LastWriteTime |
  Format-Table -AutoSize

Write-Host "=== AUTH STATE CANDIDATES ==="
Get-ChildItem "$env:APPDATA\Accio" -Recurse -File -ErrorAction SilentlyContinue |
  Where-Object { $_.Name -match 'auth|authorize|credential|connect|session' } |
  Select-Object FullName, Length, LastWriteTime |
  Format-Table -AutoSize

Write-Host "=== SHOP / CONTEXT CANDIDATES ==="
Get-ChildItem "$env:APPDATA\Accio" -Recurse -File -ErrorAction SilentlyContinue |
  Where-Object { $_.Name -match 'shop|store|pair|context|current' } |
  Select-Object FullName, Length, LastWriteTime |
  Format-Table -AutoSize

Write-Host "=== current_auth_state.json ==="
Get-Content "$env:APPDATA\Accio\current_auth_state.json" -ErrorAction SilentlyContinue

Write-Host "=== current_shop_context.json ==="
Get-Content "$env:APPDATA\Accio\current_shop_context.json" -ErrorAction SilentlyContinue

Write-Host "=== USERPROFILE .accio ==="
Get-ChildItem "$env:USERPROFILE\.accio" -Force -ErrorAction SilentlyContinue |
  Select-Object FullName, Length, LastWriteTime |
  Format-Table -AutoSize

Write-Host "=== ACCIO INSTALL PATH HINT ==="
where.exe Accio
where.exe accio

=== 结果整理模板 ===
# Windows 侧结果整理模板

把你在 Windows 机器上跑出来的证据按下面顺序填好，然后交给 Windows 侧 Codex 写 `output/current_shop_context.json`。

## 1. 授权状态
- 授权状态文件路径：
- 文件内容里表示已连接/已授权的字段：
- 是否需要重新授权：

## 2. 店铺映射
- 网页端店铺 ID：
- 网页端店铺名：
- Accio 配对店铺 ID：
- Accio 配对店铺名：
- 证据来源路径：

## 3. 周报目录
- 周报源目录：
- 周报输出目录：

## 4. 最终写入的 JSON
```json
{
  "web_shop_id": "",
  "web_shop_name": "",
  "accio_pair_shop_id": "",
  "accio_pair_shop_name": "",
  "report_source_dir": "",
  "report_output_dir": ""
}
```

## 5. 交给 Codex 的一句话
- 请根据以上本机证据，写入或更新 `output/current_shop_context.json`，不要猜值，不要使用 Mac 路径。
